// Compatibility wrapper. New code should use RepositoryProvider/useRepositories.
export { RepositoryProvider as StreamingDataProvider, useRepositories as useStreamingData } from './RepositoryProvider';
