// Kept as a compatibility entry point for future dependency injection.
// Screens now consume StreamingDataProvider, which currently serves local mock data.
export { mockCatalogItems, createMockRows } from '@/data/mockContent';
